[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "carekoala-guardian"
version = "0.1.0"
description = "Local conversation-safety guardian for CareKoala"
requires-python = ">=3.10,<3.13"

[tool.setuptools.packages.find]
include = ["boundary_ml*"]

